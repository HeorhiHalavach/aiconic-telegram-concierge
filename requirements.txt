telethon==1.44.0
cerebras_cloud_sdk==1.91.0
PyYAML==6.0.3

# только для login.py: нарисовать QR в консоли. Сторонним клиентам Telegram
# SMS не отдаёт, а код в приложение может не дойти — QR не имеет этой
# зависимости. Чистый Python, Pillow не нужен для ASCII-вывода.
qrcode==8.2

# окно администратора (этап 2). Проверено на Python 3.14.3: встают вместе с
# остальным, нативное расширение pydantic_core грузится.
fastapi==0.141.1
uvicorn==0.52.3

# только для тестов
pytest==8.4.2
pytest-asyncio==1.2.0
