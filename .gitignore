# Секреты. TG_SESSION — полноценный доступ к аккаунту.
.env
*.session
*.session-journal

# Выключатель
STOP

# Логи диалогов: там переписка с клиентами
logs/

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.tmp

# Служебное
.idea/
.vscode/

# Исходный PDF КП: содержит контакты третьего лица, в репозиторий не кладём.
# Файл остаётся в рабочей папке. Нужен в git — удалите эту строку.
*.pdf

# временные каталоги имитации
.sim-data/
.sim-STOP
